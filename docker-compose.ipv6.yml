services:
  proxy:
    sysctls:
      net.ipv6.conf.all.disable_ipv6: 0
      net.ipv6.conf.default.disable_ipv6: 0

  melodash:
    sysctls:
      net.ipv6.conf.all.disable_ipv6: 0
      net.ipv6.conf.default.disable_ipv6: 0

networks:
  melodarr-ipv6:
    name: ${DOCKER_NETWORK:-melodarr-ipv6}
    external: true
